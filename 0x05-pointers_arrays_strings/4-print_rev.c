#include "main.h"
#include <string.h>

/**
 * print_rev -  a function that prints a string, in reverse
 * followed by a new line.
 *  
 *  @s: a string ro ber printed revered
 *
*/

void print_rev(char *s)
{
	while (*s) = '\0')
	{
		_putchar(*s + 0);
		s--;
	}
		_putchar('\n');
}
